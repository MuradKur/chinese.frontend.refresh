import { FC } from 'react';
// import { useQuery } from '@apollo/client';

interface IExternalProps {}

interface IProps extends IExternalProps {}

const BrandsFilter: FC<IProps> = () => {
  // const { data } = useQuery();

  return (
    <div>
      <p>Component</p>
    </div>
  );
};

export default BrandsFilter;
